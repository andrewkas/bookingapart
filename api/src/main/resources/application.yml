spring:
 # jpa:
 #   properties:
 #     hibernate.javax.cache.provider: org.ehcache.jsr107.EhcacheCachingProvider
 #     hibernate.cache.region.factory_class: org.hibernate.cache.ehcache.EhCacheRegionFactory
 #     hibernate.cache.use_second_level_cache: true

  flyway:
    enabled: true
    ignore-missing-migrations: true
    locations: classpath:/migration
    baseline-on-migrate: true
    baseline-description: Flyway Scripts
    url: ${spring.datasource.url}
    user:  ${spring.datasource.username}
    password: ${spring.datasource.password}
  datasource:
    hikari:
      jdbc-url: "jdbc:postgresql://localhost:5432/booking_base?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC"
      maximum-pool-size: 10
      driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: "jdbc:postgresql://localhost:5432/booking_base?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC"
    driver-class-name: org.postgresql.Driver

jwtconfig:
  secret: secrets
  expiration: 300000

